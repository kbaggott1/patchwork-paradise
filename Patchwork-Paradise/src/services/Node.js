
export default class Node {
    /**
     * A node represents contains text and placement for a panel
     * @param {*} placement Placement of the node's text within the screen
     */
    constructor(placement){
        this.placement = placement;
    }
}